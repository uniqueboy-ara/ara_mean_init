import { ConfirmPasswordEqualValidatorDirective } from './confirm-password-equal-validator.directive';

describe('ConfirmPasswordEqualValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmPasswordEqualValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
